# 生产构建（APK / PWA 部署）注入的后端地址。
# 非密钥（公网 IP），故可提交入仓（.gitignore 显式 un-ignore 本文件）。
# Vite 在 `vite build`（mode=production）时自动读取本文件并 inline 进包。
# 本地 dev 走 .env.local（localhost:8787）覆盖本值；CI 无 .env.local → 用本值。
VITE_AIJI_BACKEND=http
VITE_AIJI_BACKEND_BASE=http://106.54.26.195
