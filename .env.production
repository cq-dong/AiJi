# 生产构建（APK / PWA 部署）注入的后端地址。
# 非密钥（公网 IP），故可提交入仓（.gitignore 显式 un-ignore 本文件）。
# Vite 在 `vite build`（mode=production）时自动读取本文件并 inline 进包。
# 本地 dev 走 .env.local（localhost:8787）覆盖本值；CI 无 .env.local → 用本值。
VITE_AIJI_BACKEND=https
# 2026-09-02: aiji.site 被腾讯云未备案域名阻断（443 SNI 被 DPI RST），改 IP 直连。
# IP 入口证书为自签 CA（APK 内置信任，见 res/raw/aiji_ca.pem + network_security_config.xml）。
# 域名恢复可用（备案通过或迁海外节点）后改回 https://aiji.site。
VITE_AIJI_BACKEND_BASE=https://106.54.26.195
