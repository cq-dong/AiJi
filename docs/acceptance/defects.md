# AiJi UI 验收缺陷日志

迭代循环（lead 编排，subagent 执行）：
**全量验收 → lead 整理/分诊 → fan-out 修复（按屏并行，共享文件串行）→ 全量端到端复验（重点核已修 bug 是否真修好 + 无回归）→ 重复至验收报告 LGTM。**

本文件是 fix/verify subagent 之间的共享状态（subagent 无 per-member 记忆，唯一持久的真相源是代码 + 本文件）。fix subagent 修完置 🟢，verify subagent 复验通过置 ✅，未过重开 🔴。

## 状态约定
- 🔴 open · 🟡 fixing · 🟢 fixed（待复验）· ✅ verified · ⚪ wontfix（刻意偏差，见下）

## 刻意偏差（勿报为 bug，不进缺陷表）
- home：卡标题/预览取 seed（非 Figma 占位）；今天计数=seed 真实条数（2，非 "12"）；星期按 ISO 实算；"AI 已分类" 圆点用 Figma 紫 `#7c3aed`；头像 pri 内圆保白字可读。
  - 注：曾把"真实保存后头部日期跳到真今天"列为刻意偏差——**iter1-verify 证伪**：实际并不跳（D4 时区 bug 所致）。D4 修好后才会跳，届时属正确行为。
- detail：顶栏标题=条目日期（非"条目详情"占位）；返回 ‹ 26px/light/t2；transcript 在 player 上方 ink 色；ready+errand/event 显 TodoConfirm 卡。
- search：空态用 Figma 布局（最近+建议）非居中 EmptyState；SearchBar 自动聚焦显 focus ring。
- settings：微信/QQ 用品牌色 `#12ac50`/`#1f7ccc`（spec）。
- categories：卡点击跳 `/`（过滤视图留 v2）。

## 缺陷表
| ID | 严重度 | 屏 | file:line | 描述 | 状态 | 修迭代 | 复验 |
|----|--------|----|-----------|------|------|--------|------|
| D1 | major | home/capture | src/app/store.ts:47-65; src/ui/screens/home/index.tsx:24-55 | golden path 断裂：`finishSave` 旧实现只清草稿、不落库、不置 justSaved；首页 toast 仅 `?demo=justsaved` 门控。 | ✅ verified | iter1-fix | iter1-verify |
| D2 | minor | home | src/ui/screens/home/helpers.ts:29-32 | 月份 off-by-one：`monthDayLabel` 直接打印 0-indexed `m` → 7月 seed 显示"6月"。 | ✅ verified | iter1-fix | iter1-verify |
| D3 | major | summary | src/ui/screens/summary/index.tsx:22 | scope tab 只改按钮高亮，`seedAggregates.map` 始终渲染全部聚合。 | ✅ verified | iter1-fix | iter1-verify |
| D4 | minor | home | src/ui/screens/home/helpers.ts:5-23,58-63; src/ui/screens/home/index.tsx:53 | 时区日期 bug：新条目 `createdAt=new Date().toISOString()`（UTC `Z`）vs seed `+08:00`；旧 `todayKeyFrom`/`dateKey`/`timeLabel` 裸 `slice` → UTC↔本地偏移窗口内 todayKey 取 UTC 日期、头部不跳真今天、新卡并入昨日。修：三函数改走 `new Date(iso)` 取本地年月日/时分；`todayKeyFrom` 的 max 比较由字符串改为 `getTime()` 时序（字符串比较在跨时区 ISO 上非时序，是同根第二处）；`home/index.tsx` `sorted` 同改 `getTime()` 差。store.ts 不动（存 UTC ISO 是正确的绝对瞬时）。 | ✅ verified | iter2-fix | iter2-verify |
| D5 | blocker | capture/home | src/app/store.ts:37-41,63-77 | `localSeq` 模块级、页面重载归零 → 首次 `newLocalId()` 恒产 `local-1`；`finishSave` 调 `di.storage.saveEntry` = `db.entries.put` 覆盖前次 `local-1`。iter3-verify 实测：lead 的 local-1 createdAt `2026-07-15T18:38:00.105Z` 被 verify 存盘覆盖为 `18:59:08.881Z`（内容同因 mock 文本，时间戳证覆盖；DB 仍 13 条，无 dup，但 local-1 被替）。后果：跨重载"再次保存"覆盖前会话 local-1；多会话累存时每会话前 N 条覆盖前会话前 N 条 → 用户最早条目逐会话丢失。违背 store.ts 注释"稳定递增"。修建议：hydrate 时 `localSeq = max(已存 local-N)` +1；或改 `crypto.randomUUID()`/`Date.now()`（"受限沙箱"顾虑对 PWA 多半不成立）。 | ✅ verified | iter3-fix | iter4-verify |
| D6 | minor | home | src/app/store.ts:75 | `finishSave` 把新条目 `[entry, ...s.entries]` 前插，未去重同 id 旧条目 → 存盘后内存含两条 local-1（home header 显"今天 2 条"），而 Dexie 仅 1 条（`put` 覆盖）。重载后 hydrate 读单条→计数归 1。存盘后 UI 与持久态暂时不一致。D5 修后（id 唯一）自消。 | ✅ verified | iter3-fix | iter4-verify |

## 迭代报告
- **iter1-verify (2026-07-16, 完成, LGTM)**: 全量端到端验收。3 修复全部确认（硬证据：toast 3.49s 自收、计数 2→3、卡置顶；月份"7月15日 周三"；scope 日/周/月切换 + tap-flip）。3 遗漏屏全过（search 空/匹配/过滤/无果/清空/→detail；settings 主题/定位 switch/AI 行/导出；onboarding 欢迎/key/权限/CTA→/）。回归无（home 5 变体、detail e1/e6/e7/DemoToggle/e8/不存在、categories）。控制台 0 错 0 警。token 经 `getComputedStyle` 逐项精确匹配。新增 1 minor：D4 时区日期。截图存 `.e2e_shots/`。
- **iter1-fix (2026-07-16, 完成)**: `fix` agent 修 D1/D2/D3，`npx tsc -p tsconfig.app.json` EXIT=0。lead 已读改后源码确认 3 处修复属实。变更文件：store.ts / home/index.tsx / home/helpers.ts / summary/index.tsx。未 commit/push。
- **iter2-fix (2026-07-16, 完成)**: lead 直接修 D4（单屏 2 文件 ~12 行，不值得 round-trip 子代理）。`todayKeyFrom`/`dateKey`/`timeLabel` 改 `new Date(iso)` 本地解析；补修 `todayKeyFrom` 的 max 比较与 `home/index.tsx` `sorted` 的字符串比较→`getTime()` 时序（同根第二处，iter1-verify 未单列）。`npx tsc -p tsconfig.app.json` EXIT=0。待 iter2-verify 复验。
- **iter2-verify (2026-07-16, 完成, LGTM)**: D4 复验通过（核心）。**D4 核验**：golden path 保存后头部跳到浏览器本地真今天"7月16日 周四"（TZ Asia/Shanghai +08，实测 01:56/02:00 存盘——正是 iter1 D4 触发的 UTC↔本地偏移窗口，UTC `2026-07-15T18:00Z`）；新条目并入"今天"分组（计数 1），seed 7/15 并入"昨天"，7/14 并入"7月14日 周二"绝对日期。**组内排序**：`sorted` 改 `getTime()` 后跨 +08/Z 格式时序正确——新 Z-ISO 条目置顶（旧 `localeCompare` 会误排到 e1 后，已验证）。**timeLabel**：逻辑复现 fixed 对 Z-ISO 返回 "2:00"（本地=localHM），旧裸 slice 返回 "18:00"（UTC，错）；+08 seed 两者同"8:12"无回归。**D1 无回归**：in-page click+poll 精测 toast appeared 1330ms / dismissed 4869ms / duration 3539ms ≈ 3.5s。**隔离确认**：grep 证实无他屏 import home/helpers（detail/search/categories 各有自带 helpers），D4 影响面仅 home。**回归全扫 8 屏通过**：home 5 变体（empty/justsaved/refresh/failed/offline）+ post-save；capture 录/停/存；detail e1/e6/nonexistent+DemoToggle；categories 5 卡+演示空态；summary 周/日/月 scope+tap-flip（top→正在重新生成）；search 空/匹配(地铁→e1+e9)/无果/清空+autofocus+filter chips；settings 主题亮→暗→跟随系统 active 切换+record-location switch aria-checked false→true→false+AI 行+导出；onboarding 请求权限→已授权+CTA→/。**运行时**：控制台 0 错 0 警；网络 0 4xx/5xx（1 GET 200）。**token**：getComputedStyle 逐项精确匹配（page #f7f7fa / card #fff / ink #14141a / pri #4f46e5 / priS #eeedfd / brd #ececef / Noto Sans SC / radii card 16 / fab 28）。D1-D4 全 ✅，无新缺陷、无回归。截图 `.e2e_shots/iter2_*`。
- **iter3-verify (2026-07-16, 完成, NOT-LGTM)**: Phase 2 Dexie StoragePort 全量复验。**持久化 golden path（核心）通过**：save→reload→local-1 留存于 home（firstCard "不用再开另一个 app 了"）+ `/detail/local-1` 可达（"7月16日 02:38" 本地时分）+ search "另一个 app" 可搜（"今天 02:38 · 文本"）。重载后计数归 1（存盘后瞬时内存双 local-1 见 D6，重载 hydrate 归正）。**Dexie 健康**：直接 `indexedDB.open('aiji')` 读 entries store = 13 条（12 seed e1-e12 + local-1），seed-on-empty **未重复灌入**；控制台 **0 错 0 警**（无 `[store] hydrate/saveEntry failed`、无 IDB quota/version error；3 条皆良性 vite-connect + React DevTools）。**D4 无回归**：header 本地真今天"7月16日 周四"；跨 +08/Z 排序 local-1(Z,07-16) 置顶 seed(+08,07-15) 前。**iter2 已知限制已解**：`search/helpers.ts:82` + `categories/index.tsx` `localeCompare`→`getTime()`（Phase 2 修，两屏已接 store entries）。**回归 8 屏通过**：home 5 变体（empty/justsaved/refresh/failed/offline banner 各正）+ post-save/reload；capture 文本存盘；detail local-1；categories 5 卡（local-1 无 AI 不入 latestSummary，snippets 仍 seed AI）；summary 周（top ag-w28 fresh / second ag-w27 stale-recalc）；search 空+匹配（地铁→e1+e9；另一个 app→local-1）；settings 主题/switch aria-checked false/AI 行/导出；onboarding 请求权限→已授权+CTA→/。**token** getComputedStyle 全精确（page #f7f7fa/card #fff/ink #14141a/pri #4f46e5/priS #eeedfd/brd #ececef/Noto Sans SC/radii card 16/fab 28）。**新 blocker D5**：`localSeq` 重载归零→首次保存产 `local-1`→`db.entries.put` 覆盖前次 local-1（实测 lead `18:38:00.105Z` → verify `18:59:08.881Z`）；跨重载再保存致最早条目逐会话丢失，违背"稳定递增"。**新 minor D6**：finishSave 前插未去重同 id→存盘后内存双 local-1（"今天 2 条" vs Dexie 1 条），重载归正。**观察（非缺陷，无 error）**：IDB `dbVersion=10` 而 `db.ts` 仅 `version(1)`——疑 dev 早期 schema 迭代残留；hydrate 不报错（stores 同），新用户库会 v1；建议清理或确认。**预存 quirk 确认（非本阶段引入，lead 已记 line 43）**：`search/helpers.ts` `TODAY_REF` 锚 seed 07-15，`formatRelativeTime` `dayDiff<=0` 显"今天"→ local-1(07-16, 相对 seed 为"未来") 显"今天 02:38"（巧合正确，真·未来条目会误标"今天"）。截图 `.e2e_shots/iter3_*`。**结论**：持久化能跑、无错、无回归、token 精确、D4 稳；但 D5 数据丢失阻断"累存"核心用例（记的工具前提）——NOT-LGTM，须修 `localSeq`（hydrate 取 max 已存 local-N，或 UUID/Date.now）后复验。
- **iter3-fix (2026-07-16, 完成)**: lead 直接修 D5（store.ts 单文件 2 处编辑，不值得 round-trip 子代理）。删 module-level `localSeq`+`newLocalId()`（原 store.ts:36-41）；`finishSave` 改 `id: crypto.randomUUID()`（浏览器 PWA 运行时 secure-context 可用；原"受限沙箱"注释是 Workflow 脚本约束误代入 PWA 运行时，verify 正确指出自相矛盾）。D6 自消（id 唯一→存盘后内存不再双条）。`npx tsc -p tsconfig.app.json` EXIT=0。db.ts `version(1)` 代码正确（verify 见 IDB dbVersion=10 是 dev 早期 schema 残留于本机浏览器，非代码问题；新用户库 v1）。待 iter4-verify 复验。
- **iter4-verify (2026-07-16, 完成, LGTM)**: D5/D6 复验通过（核心）。**D5 核验（累存非覆盖）**：直接 `indexedDB.open('aiji')` 读 entries store 实测 IDB 计数轨迹 13(baseline=12 seed+local-1)→save A 14→reload 14→save B 15→reload 15，**累存不丢**。两次新存 id 均 UUID：UUID-A `71df595d-3091-4ba8-84c8-d207690f9436`(`2026-07-15T19:26:18.554Z`) + UUID-B `3eff2b0f-f540-4f41-aa3a-68b911a89ed5`(`19:29:27.582Z`)，idIsUuid=true；**local-1 createdAt 全程 `18:59:08.881Z` 未变**（未被新存覆盖）。关键判别：若旧 D5 在（localSeq 重载归零→save B 产 local-1），save B 后 local-1 时间戳应变 `19:29:27Z` 且计数留 14——实测相反（local-1 不变、计数 15）→ 覆盖 bug 已除。home "今天" 3 卡 = UUID-B+UUID-A+local-1 三独立 id 并存。**D6 核验**：save A 后 home "今天 2 条"=UUID-A+local-1（异 id）；save B 后 "今天 3 条"=UUID-B+UUID-A+local-1（异 id）；无同 id 双卡（crypto 保唯一→前插不碰撞）；reload 后 hydrate 读 15 条独立→计数稳 3。D6 随 D5 自消。**回归（D4 无回归 + 已接 store 屏）**：header 本地真今天"7月16日 周四"全程；detail deep-link `/detail/71df595d-…` 可达（hydrate 解析运行时 UUID），顶栏标题"7月16日 03:26"=D4 `new Date(iso)` 本地解析 UTC `2026-07-15T19:26Z`→+08 07-16 03:26（旧 bug 显"7月15日 19:26" UTC）；search "另一个"→3 命中（UUID-B 今天 03:29 / UUID-A 今天 03:26 / local-1 今天 02:59，newest-first + 本地时分，getTime 排序）；categories 5 卡 snippet 仍 seed-AI（saved 无 AI 正确不入 latestSummary）；summary(周 ag-w28 fresh+ag-w27 stale-recalc)/settings(主题/switch/AI 行/导出)/onboarding(权限+CTA) 各渲染正常。**运行时**：console 0 错 0 警（含 preserved across navigations；无 `[store] hydrate/saveEntry failed`、无 IDB quota/version error、无 double-seed——seedCount 全程 12）。**IDB 终态**：count=15、dupIds=[]、12 seed + 1 local + 2 uuid；dbVersion=10（lead iter3-fix 已证：代码 db.ts version(1) 正确，v10 为本机 dev 早期 schema 残留，非代码问题，新用户库 v1，非缺陷）。**token** getComputedStyle 全精确（page #f7f7fa/card #fff/ink #14141a/pri #4f46e5/priS #eeedfd/brd #ececef/Noto Sans SC/radii card 16px/fab 28px）。**偏离说明（非阻断）**：task #14 要求 clean-DB reset（`indexedDB.deleteDatabase('aiji')`）以确认 v1——auto-mode 安全分类器阻断 deleteDatabase（判为 irreversibly 毁本地状态，见不到 task 授权）；改在既有 v10 baseline 上测，**证据更强**（同时证新 UUID 存不覆盖既有 local-1）；v1 重置与 D5 修复正交，lead iter3-fix 已澄清代码端 version(1) 正确。**结论**：D5 blocker 已除（累存核心用例恢复）、D6 自消、无回归、console 净、token 精确——**LGTM**。截图 `.e2e_shots/iter4_*`。

## Phase 2 — Dexie StoragePort（条目持久化）
lead 实现 + smoke-test GREEN；verify 全量复验待定。
- **范围**：entries 走 Dexie（首屏空库灌 seed，保存即落库，重载留存）。entryAi/categories/aggregates/tags/settings 仍 seed 直读（其写入路径 LLM port/设置 UI 未落地，待后续端口接入再路由 Dexie）。
- **变更文件**：`ports/index.ts`(+`saveEntry`) · `adapters/dexieStorage.ts`(新；entries=Dexie+seed-on-empty，余=seed) · `adapters/mockStorage.ts`(删) · `app/di.ts`(swap→dexieStorage) · `app/store.ts`(+`hydrate()`/`hydrated`；`finishSave` 落库 `di.storage.saveEntry`) · `main.tsx`(boot 调 hydrate) · `detail/index.tsx`+`search/index.tsx`+`categories/index.tsx`(改读 store entries) · `search/helpers.ts:82`+`categories/index.tsx:16` 的 `localeCompare`→`getTime()`（已知限制条目已解）。
- **lead smoke-test (2026-07-16, GREEN)**：save→reload→persist ✅（"不用再开另一个 app 了" 留存于"今天"）；detail deep-link `/detail/local-1` ✅（hydrate 解析）；console 0 错；D4 无回归（header "7月16日 周四"、timeLabel "02:38" 本地）。`npx tsc -p tsconfig.app.json` EXIT=0。
- **verify 复验基线（重要）**：Dexie 当前含 12 seed + 1 lead 测试条目 `local-1`（"不用再开另一个 app 了"，status=processing，07-16，无 AI）。此为 lead 测试残留，**非 bug**。预期：home header "7月16日 周四"（local-1 最新 → `todayKeyFrom` 取 07-16）；"今天"=1(local-1)；"昨天"=seed 07-15(e1,e2)；余按 07-14/13/12/11 分组。此为 D4+Dexie 正确行为，非 iter2 07-15 基线回归。verify 须自做一次 save→reload→persist 复测新功能（2nd 条目）。

## Phase 3 — CapturePort（PWA 采集）
lead 实现 + desktop self-verify GREEN；verify 全量复验待定。
- **范围**：capture 走 webCapture（getUserMedia 麦克风 + WebSpeech 实时预览 + MediaRecorder 录音 blob 留内存会话内回放）。SttPort(Whisper 终稿)/LlmPort/OPFS 媒体持久化仍后置。store `startRecording`/`stopRecording` 由 flag-only 改为真实调 `di.capture`；`CaptureDraft` +`finalized`/`interim`（实时转写分段）；capture 屏 `RecordingView` 显 `liveTranscript`（去硬编码 mock 转写），`onStop` 只调 `stopRecording`（store 构 audio part 含 ref/durationSec/transcript）。
- **变更文件**：`ports/index.ts`(`CapturePort.startAudio`+`onInterim`/`onFinal` 回调) · `adapters/webCapture.ts`(新) · `app/di.ts`(注入 capture=webCapture) · `app/store.ts`(startRecording/stopRecording 真实+finalized/interim；stopRecording 构 audio part) · `ui/screens/capture/index.tsx`(读 finalized/interim→liveTranscript；onStop 只 stopRecording；删 SAMPLE_TRANSCRIPT) · `ui/screens/capture/variants.tsx`(CaptureApi+liveTranscript；RecordingView 显 liveTranscript)。
- **lead desktop self-verify (2026-07-16, GREEN)**：fake-mic 注入（AudioContext 振荡器 MediaStream + no-op SpeechRecognition，绕桌面 MCP 无法授权麦克风）验采集状态机：record→RecordingView("录音中·00:08"计时+转写占位"…")→stop→MultiView("1 片段·语音 00:28")→保存→home("今天 4 条"含新音频条"未命名")→reload 留存。IDB 直读：16 条(12 seed+3 前测+1 新音频)，新音频条 id=UUID `d2e6f13b-…`、part=[audio]、ref=`audio-8b409a0…`、durationSec=28、transcript 空(no-op SR 无语音，预期)、status=processing。console 1 错(AudioContext 振荡器设备错误，fake-mic 副作用非 AiJi 代码)。`npx tsc -p tsconfig.app.json` EXIT=0。
- **验收限制（重要）**：桌面 MCP 无法授权原生麦克风权限(perm=「prompt」阻塞)，真实 getUserMedia+WebSpeech 实时转写路径(A1)须手机验(iOS Safari/Android Chrome)。fake-mic 注入仅验 store↔adapter↔屏 状态机接线，非浏览器麦克风实现。空转写→home 卡显"未命名"是下游展示瑕疵(SttPort/Whisper 后置)，非本阶段 bug。
- **iter5-verify (2026-07-16, 完成, LGTM)**: Phase 3 CapturePort 全量复验 + 8 屏回归。**A 录音 golden path（核心）通过**：fake-mic 注入（AudioContext 振荡器 MediaStream + no-op SpeechRecognition，绕桌面 MCP 无法授权麦克风）`{patched:true,tracks:1}` → 点"开始语音录制"→ RecordingView("语音录制"标题+"录音中 · 00:07"→"00:26"计时递增证实 ticking+"正在转写"+`…`占位[空 liveTranscript，FakeSR 无 onresult，预期]+"停止录音")→ 点停止 → MultiView("新条目"+"1 片段"pill+"保存"+PartsStack "语音 · 00:40"[durationSec=40,Math.max(1,round)])→ 保存 → home("今天 5 条"含新"未命名"卡置顶[空转写 fallback，预期])→ reload → "今天 5 条"留存。**IDB 核心证据**：直读 `indexedDB.open('aiji')` entries store，baseline 16→save 后 17→reload 17，**新音频条 id=UUID `fe9b11a6-a332-4b3a-bcf7-e444cb4d8be2`（idIsUuid=true）**、`status:'processing'`、`parts:[{type:'audio', ref:'audio-630ed8b7-5f50-43c7-8977-14830d3ab0fb', durationSec:40, transcript:''}]`（ref=`audio-<uuid>` 格式、durationSec≥1、空转写[FakeSR 预期]）→ **store↔adapter↔屏 状态机接线端到端通**。**B NoMicView（渲染+真实 catch 路径）通过**：任务书提议从 EmptyView 点"切换"调 denyMic()，但 **EmptyView 的 TopBar 是 `kind:"capture"` 不渲"切换"**（"切换"只在 `kind:"record"`=RecordingView/NoMicView 出现，variants.tsx:50-72）→ 改用更严格的真实路径：override `getUserMedia` 抛 `NotAllowedError` → `webCapture.startAudio` throw（webCapture.ts:56）→ `store.startRecording` catch（store.ts:63-66）置 `micDenied:true` → NoMicView 渲染（"语音录制"标题+"麦克风未授权"+"未获得麦克风权限…可改用文本记录"+"改用文本"+"去设置"）→ 点"改用文本"（onUseText）→ allowMic+mode=text → 回 EmptyView。**真实 getUserMedia 桌面挂起确认**：未 override 时点 record，30s 内既无"录音中"也无"麦克风未授权"（getUserMedia 卡原生 prompt，非 throw 非 resolve）——印证"桌面无法授权麦克风"限制，catch 路径桌面不可达，故用 override-throw 验证 catch 块代码正确。**C 文本模式 save 回归通过**：fresh /capture 点"文本"pill（onPickMode→addPart text "不用再开另一个 app 了"）→ MultiView("1 片段"+"保存")→ save → home("今天 6 条"+1)→ reload 留存。IDB：total 18、新文本条 UUID `3b911226-d20a-4502-8319-4bdf68bf99d3`、status:processing、parts:[{type:'text',content:'不用再开另一个 app 了'}]。**D 回归 8 屏（D1–D6 全 ✅ 无回归）**：home post-save/reload（header 本地真今天"7月16日 周四"全程、今天/昨天/7月14日/13/12/11 分组正、D4 稳）+ post-save toast 流；detail deep-link `/detail/fe9b11a6-…`（hydrate 解析运行时 UUID，顶栏"7月16日 04:13"=D4 `new Date(iso)` 本地解析 UTC `2026-07-15T20:13:02.520Z`→+08 07-16 04:13，旧 bug 显"7月15日 20:13" UTC）+"语音 00:40"audio part + AI 处理中；`/detail/e1` seed（"7月15日 08:12" +08 seed 本地解析 + transcript + AI 面板 deepseek/类别想法/标签/侧面）；categories 5 卡（想法18/项目进展12/生活片段9/阅读笔记5/待办7）snippet 仍 seed-AI（保存条目无 AI 正确不入 latestSummary）；summary 日/周/月 scope 切换（D3：日只显 day 聚合、月"暂无摘要"空态、不再 .map 全部）+ 点顶"本周"卡 tap-flip→"加载中 正在重新生成本周摘要…"；search 空（textbox 自动聚焦 focus ring+最近+建议 Figma 布局）/匹配（"地铁"→e1+e9 两命中）/保存条目可搜（"另一个"→4 命中 newest-first 本地时分 04:18/03:29/03:26/02:59，getTime 排序+D4 本地时 label）/无果（"没有匹配「zzznomatch」的条目"）/清除→回空态；settings 主题亮→暗 active 移动+record-location switch aria-checked false→true+AI 行(DeepSeek BYOK/Whisper 云端)+导出(MD/zip+微信/QQ)；onboarding 欢迎+key+权限+"开始记"CTA→/。home demo-only 变体（empty/failed/offline）未再驱动——Phase 3 不碰 home 渲染/online 状态，无回归面，核心 post-save/reload 路径已验。**E console+token**：`list_console_messages`(includePreservedMessages, error+warn) 全流 **0 错 0 警**（无 `[store] hydrate/saveEntry/startAudio/stopAudio failed`、无 IDB quota/version error、无 double-seed——seedCount 全程 12；本轮 fake-mic AudioContext 设备错亦未现）。**token** getComputedStyle 全精确：page rgb(247,247,250)=#f7f7fa / card rgb(255,255,255)=#fff / ink rgb(20,20,26)=#14141a / pri rgb(79,70,229)=#4f46e5 / priS rgb(238,237,253)=#eeedfd / brd rgb(236,236,239)=#ececef / rounded-card 16px / rounded-fab 28px / fontFamily "Noto Sans SC",system-ui,sans-serif。**TS**：`npx tsc -p tsconfig.app.json` EXIT=0。**IDB 终态**：count=18、dupIds=[]、12 seed + local-1 + 5 UUID(71df/3eff/d2e6f13b[lead audio]/fe9b11a6[本轮 audio]/3b911226[本轮 text])、local-1 全程留存未被覆盖（D5 累存稳：16→17→18，零覆盖零 dup）、dbVersion=10（lead iter3-fix 已证：代码 db.ts version(1) 正确，v10 本机 dev 残留，非缺陷）。**新缺陷**：无（D7+ 无）。**结论**：CapturePort 状态机接线端到端通（录音→停止→构 audio part→落库→留存，IDB 硬证据）、NoMicView 真实 catch 路径渲对、D1–D6 全 ✅ 无回归、console 净、token 精确、tsc 0——**LGTM**。截图 `.e2e_shots/iter5_*`。

## Phase 4 — OPFS 媒体持久化 + 回放（A2 代码脱险）
lead 实现 + desktop self-verify GREEN；verify 全量复验待定。
- **范围**：录音 blob 不再丢——`webCapture.stopAudio` 返回 `blob`；`store.stopRecording` 经 `di.storage.saveMedia(ref, blob)` 落 OPFS；`detail/PartView` 的 `AudioPlayer` 从 OPFS 取 blob→`URL.createObjectURL`→`<audio>` 真实播放（播/暂停切换、时长显、ended 复位）。Seed 音频 part 无 blob → player disabled（降级，不崩）。SttPort(Whisper 终稿)/LlmPort 仍后置。
- **变更文件**：`ports/index.ts`(`StoragePort`+`saveMedia`/`getMedia`；`CapturePort.stopAudio` 返回 `+blob?`) · `adapters/dexieStorage.ts`(+OPFS `saveMedia`/`getMedia`，OPFS 不支持则 no-op/undefined 降级) · `adapters/webCapture.ts`(stopAudio 返回 blob；删内存 blobs Map + getAudioBlob 未用导出) · `app/store.ts`(stopRecording result `+blob?`；`if (result.blob) void di.storage.saveMedia(...)`) · `ui/screens/detail/PartView.tsx`(功能 AudioPlayer：useEffect 取 OPFS blob→objectURL→`<audio>`；toggle 播/暂停；revoke 清理；PlayTriangle/PauseIcon/Waveform；seed 无 blob→disabled)。
- **lead desktop self-verify (2026-07-16, GREEN)**：fake-mic 注入（AudioContext 振荡器 MediaStream，绕桌面 MCP 无法授权麦克风）→ record ~40s → stop → 保存 → home 置顶新音频条 → reload 留存 → `/detail/<uuid>` → AudioPlayer 从 OPFS 取 blob → **点播放，audio 实播**：`paused:false`、`currentTime` 递增、`duration:41.04`、`readyState:4`(HAVE_ENOUGH_DATA)、`error:null`、`srcType:blob:http://...`（Chrome 嗅 webm 字节解码，OPFS 存裸字节丢 MIME `.type=""` 不影响 webm 播放）。OPFS 回读 blob 大小 ~662KB。console 0 错 0 警（无 `[dexieStorage] saveMedia/getMedia failed`、无 OPFS quota error）。`npx tsc -p tsconfig.app.json` EXIT=0。
- **验收限制（重要）**：桌面 MCP 无法授权原生麦克风(perm=prompt 阻塞)，fake-mic 注入仅验 store↔adapter↔OPFS↔player 接线，非浏览器麦克风实现。真实 getUserMedia+录音+OPFS 持久化跨重载/配额/iOS Safari/Android Chrome 路径(A1+A2)须手机验。空转写→home 卡显"未命名"是下游展示瑕疵(SttPort/Whisper 后置)，非本阶段 bug。
- **iter6-verify (2026-07-16, 完成, LGTM)**: Phase 4 OPFS 媒体持久化+回放全量复验 + 8 屏回归。**A 录音 golden path（核心）通过**：fake-mic 注入（AudioContext 振荡器 MediaStream + no-op SpeechRecognition，绕桌面 MCP 无法授权麦克风）`{patched:true,tracks:1}`（navigate 重载后重新注入确认）→ 点"开始语音录制"→ RecordingView("语音录制"标题+"录音中 · 00:12"→wait_for "00:4"→"00:40"计时递增证实 ticking+"正在转写"+`…`占位[空 liveTranscript，FakeSR 无 onresult，预期]+"停止录音")→ 点停止 → MultiView("新条目"+"1 片段"pill+"保存"+PartsStack "语音 · 00:56"[durationSec=56，stop 时实际已录到 56s，Math.max(1,round) 正确])→ 保存 → home("今天 8 条"含新"未命名"卡置顶[空转写 fallback，预期]，7→8 +1)。**B OPFS 回读+回放（核心）通过**：进 `/detail/17ecff0f-aa15-4cba-9278-306f5a8257bb` → detail 渲"7月16日 04:55"顶栏(D4 本地解析 UTC `2026-07-15T20:55:04.627Z`→+08 07-16 04:55)+"2026-07-16 04:55 · 语音 00:56"audio part+"播放"按钮(功能 AudioPlayer)+"AI 处理"面板(处理中) → 点"播放"→ 按钮变"暂停"(toggle) → `evaluate_script` 核 **audio 实播**：`found:true`、`paused:false`、`currentTime:17.25`(递增，wait_for+evaluate 往返延迟致已播 17s，正常)、`duration:55.44`(≈56 durationSec)、`readyState:4`(HAVE_ENOUGH_DATA)、`error:null`、`networkState:1`、`srcType:"blob:http://localhost:5173/d8b66c8f..."`(OPFS blob→URL.createObjectURL) → 再点"暂停"证 `paused:true`。**OPFS blob 存在性硬证据**：`navigator.storage.getDirectory()`→`getFileHandle('audio-cc0a2472-f716-4137-b8fc-965fd51bbd15')`→`getFile()` 返回 `{found:true, size:893878, type:"", name:"audio-cc0a2472..."}`（~873KB 真实音频字节；空 MIME=OPFS 存裸字节丢 MIME，Chrome 嗅 webm 字节可播，非 bug）。**saveMedia→OPFS→getMedia→AudioPlayer 全链通**。**交叉证**：lead Phase4 self-verify 音频条 9db5857d 的 OPFS blob `audio-0eac3e2f...` 亦 `found:true, size:662038`（≈662KB，与 lead self-verify 报告一致）——独立证 OPFS 路径非仅本轮。**C 跨重载留存通过**：reload `/` → home "今天 8 条"不变(留存)+header "7月16日 周四"(D4 稳) → 再进 `/detail/17ecff0f-...` → "播放"按钮重载后重新从 OPFS 取 blob → 点播放 → `paused:false`/`currentTime:9.1`(递增)/`duration:55.44`(同重载前)/`readyState:4`/`error:null`/新 blob URL `b1570fee...`（OPFS 跨重载可读）。**D IDB 硬证据通过**：直读 `indexedDB.open('aiji')` entries store：**count 轨迹 19(baseline=12 seed+local-1+6 UUID[iter4 71df/3eff + iter5 d2e6f13b/fe9b11a6/3b911226 + lead-Phase4 9db5857d])→20(本轮 audio save)→21(本轮 text 回归 save)**、**dupIds:0**（零 dup）、**newestAudio id=UUID `17ecff0f-aa15-4cba-9278-306f5a8257bb`(idIsUuid:true)**、`status:'processing'`、`parts:[{type:'audio', ref:'audio-cc0a2472-f716-4137-b8fc-965fd51bbd15', durationSec:56, transcript:''}]`（ref=`audio-<uuid>` 格式、durationSec≥1、空转写[FakeSR 预期]）→ **store↔adapter↔OPFS↔player 接线端到端通**。**D5 累存不丢（核心）**：local-1 createdAt `2026-07-15T18:59:08.881Z` 全程未变（7 次后续 UUID 存盘零覆盖——crypto 保唯一→前插不碰撞→`db.entries.put` 不覆盖既有 id）；IDB count 单调 +1（19→20→21，零覆盖零 dup）。**E 回归 8 屏（D1–D6 全 ✅ 无回归）通过**：home 5 变体（?demo=empty "还没有记下任何东西"空态 / ?demo=failed "AI 处理失败·网络或模型异常·重试" banner / ?demo=offline "◎ 离线·待联网补跑" banner / justsaved post-save toast 流 8→9 / refresh reload 留存）+ header 本地真今天"7月16日 周四"全程 + 今天/昨天/7月14日/13/12/11 分组(D4 跨 +08/Z 排序稳)；capture 文本模式 save（点"文本"pill→addPart 默认"不用再开另一个 app 了"→MultiView"1 片段"→保存→home"今天 9 条"+1→reload 留存）+ NoMicView 真实 catch 路径（override getUserMedia 抛 NotAllowedError→startAudio throw→store catch→micDenied:true→NoMicView 渲"语音录制"+"麦克风未授权"+"未获得麦克风权限…"+"改用文本"+"去设置"→点"改用文本"→allowMic+mode=text→回 EmptyView）；detail `/detail/e1` seed（顶栏"7月15日 08:12"+08 本地解析 D4 稳 + transcript"地铁里想到…"+ AI 面板 deepseek-chat·20小时前/类别想法/标签 AiJi·地铁/侧面地点·地铁/标题/摘要+"播放"**disabled**[seed 无 blob→AudioPlayer 降级，不崩，预期]）+ 本轮 `/detail/17ecff0f-...`（audio part+AI 处理中+功能 AudioPlayer 播放）；categories 5 卡（想法18/项目进展12/生活片段9/阅读笔记5/待办7，"51 条"=seed 计数）snippet 仍 seed-AI（保存条目无 AI 正确不入 latestSummary）；summary 日/周/月 scope 切换（D3：日只显"今日 7/15"day 聚合、月"本月 暂无摘要"空态、不再 .map 全部）+ 点顶"本周"卡 tap-flip→"加载中 正在重新生成本周摘要…"；search 空（textbox 自动聚焦 focus ring+最近+建议 Figma 布局）/匹配（"地铁"→e1+e9 两命中）/保存条目可搜（"另一个"→5 命中 newest-first 本地时分 05:00/04:18/03:29/03:26/02:59，含 local-1 02:59 + 本轮 text 05:00，getTime 排序+D4 本地时 label）/无果（"没有匹配「zzznomatch」的条目"）/清除→回空态；settings 主题亮→暗 active 移动（"暗色" bg-pri text-white，"亮色/跟随系统" border-brd）+ record-location switch aria-checked false→true+AI 行(DeepSeek BYOK/Whisper 云端)+导出(MD/zip+微信/QQ)；onboarding 欢迎+key+权限+"开始记"CTA→/。**F console+token**：`list_console_messages`(includePreservedMessages, error+warn) 全流 **0 错 0 警**（无 `[store] hydrate/saveEntry/startAudio/stopAudio/saveMedia failed`、无 `[dexieStorage] saveMedia/getMedia failed`、无 IDB quota/version error、无 OPFS error、无 double-seed——本轮 fake-mic AudioContext 设备错亦未现）。**token** getComputedStyle 全精确：page rgb(247,247,250)=#f7f7fa / card rgb(255,255,255)=#fff / ink rgb(20,20,26)=#14141a / pri rgb(79,70,229)=#4f46e5 / priS rgb(238,237,253)=#eeedfd / brd rgb(236,236,239)=#ececef / rounded-card 16px / rounded-fab 28px / fontFamily "Noto Sans SC",system-ui,sans-serif。**TS**：`npx tsc -p tsconfig.app.json` EXIT=0。**IDB 终态**：count=21、dupIds=0、12 seed + local-1 + 8 UUID(71df/3eff/d2e6f13b/fe9b11a6/3b911226/9db5857d[lead-Phase4 audio]/17ecff0f[本轮 audio]/119beafa[本轮 text 回归])、local-1 全程留存 `18:59:08.881Z` 未被覆盖（D5 累存稳：19→20→21，零覆盖零 dup）、dbVersion=10（lead iter3-fix 已证：代码 db.ts version(1) 正确，v10 本机 dev 残留，非缺陷）。**新缺陷**：无（D7+ 无）。**结论**：OPFS 媒体持久化+回放端到端通（录音 blob→saveMedia→OPFS→getMedia→AudioPlayer 真实播放，audio `paused:false`/`readyState:4`/`error:null` + OPFS blob found 893878 字节 + 跨重载可读）、store↔adapter↔OPFS↔player 接线端到端通、D1–D6 全 ✅ 无回归、console 净、token 精确、tsc 0——**LGTM**。截图 `.e2e_shots/iter6_*`（recording_40s / multiview_audio_part / detail_audioplayer_playing / search_saved_entries / home_9today）。

## 已知限制（非活跃，后续数据接入时修，勿在当前验收报为 bug）
- ~~`search/helpers.ts:82` + `categories/index.tsx` localeCompare~~ → ✅ 已在 Phase 2 修（`getTime()`，两屏已接 store entries）。
- entryAi/categories/aggregates/tags/settings 仍 seed 直读（未走 Dexie）——待 LlmPort/设置 UI 落地其写入路径时再路由 Dexie。当前只读 stub，不影响功能。
- `search/helpers.ts:17` `TODAY_REF` 仍 seed 锚定（非 store 锚定）——仅影响 search 空态相对时间显示的小瑕疵，非活跃；后续可改 store-anchored。
