// Build-time constants injected by vite `define` (see vite.config.ts).
// __APP_VERSION__ is read from package.json at config time and stringified,
// so the running app carries its version without a runtime fetch.
declare const __APP_VERSION__: string
