# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets / local env (BYOK keys — never commit). *.local above covers
# .env.local; these lines add belt-and-suspenders for plain .env + variants.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite cache
.vite/

# Session memory buffer
.remember/

# E2E acceptance screenshots (regenerable artifacts)
.e2e_shots/
# Verification/acceptance screenshots dropped in repo root by review subagents
/*.png

# Claude Code session + browser automation artifacts (throwaway, not for repo)
.claude/
.playwright-mcp/

# Throwaway STT/LLM probe scripts + test audio (exploration only, no keys)
scripts/probe-*.mjs
public/asr-test.wav
